# Arduino-Based-Line-Follower-Robot
The robot has two motors (left and right) connected to Arduino via control pins (IN1, IN2, IN3, IN4).  Motor speed is controlled using PWM pins (speedL, speedR).  Two IR sensors (sensorL, sensorR) detect the line on the surface.  Arduino continuously reads the sensor values and decides the robot's movement.
